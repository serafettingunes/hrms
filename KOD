-- This script was generated by a beta version of the ERD tool in pgAdmin 4.
-- Please log an issue at https://redmine.postgresql.org/projects/pgadmin4/issues/new if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE public."Personel"
(
    id integer NOT NULL,
    "emailAdres" character varying(20) NOT NULL,
    sifre character varying(10) NOT NULL,
    "sifreTekrar" character varying(10) NOT NULL,
    PRIMARY KEY (id)
);

CREATE TABLE public."isArayan"
(
    "isId" integer NOT NULL,
    "tcNo" character varying(11) NOT NULL,
    "dogumYili" character varying(4) NOT NULL,
    PRIMARY KEY ("isId")
);

CREATE TABLE public."isVeren"
(
    "isVerenId" integer NOT NULL,
    "sirketAdi" character NOT NULL,
    "webSitesi" character NOT NULL,
    telefon character NOT NULL,
    PRIMARY KEY ("isVerenId")
);

CREATE TABLE public."sistemPersonel"
(
    id integer NOT NULL,
    PRIMARY KEY (id)
);

CREATE TABLE public.ortak2
(
    id integer NOT NULL,
    ad character NOT NULL,
    soyad character NOT NULL,
    PRIMARY KEY (id)
);

CREATE TABLE public.iss
(
    id integer NOT NULL,
    genelis character varying(50) NOT NULL,
    PRIMARY KEY (id),
	UNIQUE (genelis)
);

CREATE TABLE public.mernis
(
    id integer NOT NULL,
    "dogruYanlis" boolean NOT NULL,
    PRIMARY KEY (id)
);

CREATE TABLE public."sirketKontrol"
(
    id integer NOT NULL,
    "ePosta" character NOT NULL,
    dogrula boolean NOT NULL,
    PRIMARY KEY (id)
);

ALTER TABLE public."Personel"
    ADD FOREIGN KEY (id)
    REFERENCES public."isArayan" ("isId")
    NOT VALID;


ALTER TABLE public."Personel"
    ADD FOREIGN KEY (id)
    REFERENCES public."isVeren" ("isVerenId")
    NOT VALID;


ALTER TABLE public."isArayan"
    ADD FOREIGN KEY ("isId")
    REFERENCES public.ortak2 (id)
    NOT VALID;


ALTER TABLE public."sistemPersonel"
    ADD FOREIGN KEY (id)
    REFERENCES public.ortak2 (id)
    NOT VALID;


ALTER TABLE public."isArayan"
    ADD FOREIGN KEY ("isId")
    REFERENCES public.mernis (id)
    NOT VALID;


ALTER TABLE public."isVeren"
    ADD FOREIGN KEY ("isVerenId")
    REFERENCES public."sirketKontrol" (id)
    NOT VALID;


ALTER TABLE public."isArayan"
    ADD FOREIGN KEY ("isId")
    REFERENCES public.iss (id)
    NOT VALID;

END;
